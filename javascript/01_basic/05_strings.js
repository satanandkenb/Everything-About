const name = "Satanad Ken"
const repoCount = 40

console.log(`Name : ${name},Repository count :${repoCount}`);

const gameChange = new String("Satanand Ken")

// console.log(gameChange.length);
console.log(gameChange.charAt(5)); 
console.log(gameChange.indexOf("K"));

// Purpose                           |  Method         |  Example                 |  Description                               
// ----------------------------------+-----------------+--------------------------+--------------------------------------------
// Get characterat a specific index  |  charAt(index)  |  "hello".charAt(1)→'e'   |  Returns the character at position1        
// Get theindex of a character       |  indexOf(char)  |  "hello".indexOf('e')→1  |  Returns the first index of'e'in the string 
